## Unit Tests
- [x] `constructor`
  - [x] sets `vetoOverrideRole` correctly
  - [x] sets `vetoOverrideDuration` correctly
- [x] `_setVetoOverrideRole`
  - [x] sets `vetoOverrideRole` correctly
  - [x] emits event `VetoOverrideRoleSet`
- [x] `_setVetoOverrideDuration`
  - [x] sets `vetoOverrideDuration` correctly
  - [x] emits event `VetoOverrideDurationSet`
- [x] `overrideVeto`
  - [x] when called by any address other than the `vetoOverrideRole`
    - [x] reverts with `GovernorVetoOverride__Unauthorized`
  - [x] when called by the `vetoOverrideRole`
    - [x] when proposal exists
      - [x] sets `isVetoOverridden` of a proposal to `true`
      - [x] emits `VetoOverridden`
    - [x] when proposal does not exist
        - [x] sets `isVetoOverridden` to `true`
        - [x] emits `VetoOverridden`
- [ ] `state`
  - [ ] when proposal state is not `Defeated`
    - [ ] returns original state
  - [x] when proposal state is `Defeated`
    - [x] when veto is not overridden
      - [x] regardless if veto duration has expired
        - [x] returns `Defeated`
    - [x] when veto is overridden
      - [x] when veto duration has expired
        - [x] returns `Defeated`
      - [x] when veto duration has not expired
        - [x] returns `Succeeded`

## Integration Tests
- [ ] `setVetoOverrideRole`
- [ ] `setVetoOverrideDuration`